# 依赖与构建产物
node_modules/
dist/
*.tgz
*.tsbuildinfo

# 运行时凭据产物：src/index.ts 的 startClient() 会把这两个文件写到仓库根，
# 内含 WEIXIN token / account_id / sync_buf 明文 —— 不要提交。
client-config.json
account.json

# 管理台运行期状态：本机访问令牌与日志
admin/.admin-token
admin/*.log

# 编辑器 / 系统
.DS_Store
.dsh/
.dsh-home/
